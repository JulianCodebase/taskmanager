server:
  port: 8080
spring:
  application:
    name: task-service
  datasource:
    url: jdbc:mysql://localhost:3307/taskdb
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect